//
// Created by Arthur Gilfanov on 4/23/26.
//

#include "Frontend.h"

void Frontend::run() {

    // the run loop of actions ends up being too frontend specific so different frontend behaviours different run loops

    // first start the run loop of the UI itself

    // get user input string from ui, blocking until get something

    // tokenize it via session
        // tkns = session.tokenize(string)
    // send tokens to orchestrator via comm

    // probe for incoming confirmed tokens


    // detokenize via session
        //

    // push text to ui for display

}
